# Estimate transforms using Extended Kalman Filter
## Instructions
* Generate sensor data (IMU and Vicon) by running `quad_dyn_main`. The data is stored in `/matlab/sensor_models/DataLogging`.
* Now, run `estimate_transforms`.
